# TaskBoard prototype (archived)

The original Create React App kanban prototype. Kept for reference: its lane/card
concepts and demo data shapes seeded the Homestead task module (see
`docs/REQUIREMENTS.md` section 7). It is no longer built or deployed — CRA and
`react-beautiful-dnd` are unmaintained. The live application is in `apps/`.
